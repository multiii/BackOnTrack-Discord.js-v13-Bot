"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.emitRegistryError = void 0;
const pieces_1 = require("@sapphire/pieces");
const Events_1 = require("../../types/Events");
/**
 * Opinionatedly logs the encountered registry error.
 * @param error The emitted error
 * @param command The command which had the error
 */
function emitRegistryError(error, command) {
    const { name, location } = command;
    const { client, logger } = pieces_1.container;
    if (client.listenerCount(Events_1.Events.CommandApplicationCommandRegistryError)) {
        client.emit(Events_1.Events.CommandApplicationCommandRegistryError, error, command);
    }
    else {
        logger.error(`Encountered error while handling the command application command registry for command "${name}" at path "${location.full}"`, error);
    }
}
exports.emitRegistryError = emitRegistryError;
//# sourceMappingURL=emitRegistryError.js.map