"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CorePrecondition = void 0;
require("../lib/errors/Identifiers");
const Precondition_1 = require("../lib/structures/Precondition");
class CorePrecondition extends Precondition_1.AllFlowsPrecondition {
    messageRun(message) {
        // `nsfw` is undefined in DMChannel, doing `=== true`
        // will result on it returning `false`.
        return Reflect.get(message.channel, 'nsfw') === true
            ? this.ok()
            : this.error({ identifier: "preconditionNsfw" /* Identifiers.PreconditionNSFW */, message: 'You cannot run this message command outside NSFW channels.' });
    }
    async chatInputRun(interaction) {
        const channel = await this.fetchChannelFromInteraction(interaction);
        // `nsfw` is undefined in DMChannel, doing `=== true`
        // will result on it returning `false`.
        return Reflect.get(channel, 'nsfw') === true
            ? this.ok()
            : this.error({ identifier: "preconditionNsfw" /* Identifiers.PreconditionNSFW */, message: 'You cannot run this chat input command outside NSFW channels.' });
    }
    async contextMenuRun(interaction) {
        const channel = await this.fetchChannelFromInteraction(interaction);
        // `nsfw` is undefined in DMChannel, doing `=== true`
        // will result on it returning `false`.
        return Reflect.get(channel, 'nsfw') === true
            ? this.ok()
            : this.error({ identifier: "preconditionNsfw" /* Identifiers.PreconditionNSFW */, message: 'You cannot run this context menu command outside NSFW channels.' });
    }
}
exports.CorePrecondition = CorePrecondition;
//# sourceMappingURL=NSFW.js.map